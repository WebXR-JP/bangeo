# BANGEO

WebXRに関する日本語情報サイト。配信アプリは `apps/blog/`（Next.js）、記事は `apps/blog/content/blog/`。このファイルはCodexと他のエージェントで共有する。

## 進め方と判断の境界

依頼された成果を実装・必要な検証・検出した問題の修正まで完成させる。最初の実装だけで止めない。調査や助言のみの依頼ではその範囲を守る。

- 読み取り、ローカル編集、作業ブランチ、対象のビルド・チェックと修正は、依頼の範囲で進める。手順ごとのGoサインは不要。
- 会話で決まった要件・承認を引き継ぐ。名前、ファイル分割など戻せる判断は既存実装に合わせて進める。結果を大きく変える未決事項だけ質問する。
- push・PR作成は依頼で許可された範囲で行う。mainへの反映、本番公開、課金、第三者へのメッセージ送信、本番・共有データの削除は、その操作が未承認なら具体的な差分・影響を用意して確認する。既存の承認を取り直さない。
- このガイドは実行環境の権限を拡張しない。アクセス制御や承認機構を回避しない。

記載するリポジトリ内パスはルート基準。不要になった作業用ファイルの削除は、依頼されたローカル編集に含む。

## 必要なときだけ読む

| 作業 | 参照先 |
| --- | --- |
| 記事の新規作成・内容改訂・公開前レビュー | `.claude/skills/bangeo-article-authoring/SKILL.md` |
| 新規デモ・デモの機能変更 | `docs/agent-guides/demos.md` → 選んだ方式のスキル |
| 仕様カタログ・スターターの変更 | `docs/agent-guides/catalog.md` |
| 定期ウォッチ・イベント更新 | `docs/agent-guides/event-watch.md` |
| 匿名実験レポート連携 | `docs/experiment-analytics.md` |

軽微な誤字修正に全スキルや参照文書を読まない。スキル本体は `.claude/skills/` に一本化し、Codexは `.agents/skills/` のリンクから発見する。

## BANGEOの品質基準

- 読者向けの自然な日本語で書く。公的な技術名称を保持し、必要な説明を添える。記事に絵文字やエージェント向けの編集メモを入れない。
- 最新情報、対応状況、仕様ステータスは一次情報と確認日を根拠にする。WebARとWebXR、APIの存在確認と実機での動作確認を混同しない。
- MDXの分類・スキーマは既存実装に合わせる。公開リポジトリに秘密情報や非公開データを入れない。
- SEO関連のページ・メタデータ・内部リンクを変更するときは `apps/blog/src/app/sitemap.ts` の実際の対象を確認し、構造更新に対応する `SITE_STRUCTURE_UPDATED` を更新する。記事追加だけで無関係なページを変更しない。

## 検証と完了

ルートのパッケージ管理は `package.json` のpnpm指定に従う。個別デモはその `package.json` とlockfileを確認する。

- `pnpm run check:ci`: Biomeの読み取りチェック。`check` と `format` は書き換えるので対象外の整形差分を混ぜない。
- `pnpm -C apps/blog build`: 画像最適化とNext.jsビルド。画像にも変更が出るため差分を確認する。
- UIやデモ変更では、使える環境で表示・操作・アセット読込を確認する。実機がなければ可能な検証を完了し、未確認の端末・機能を報告する。

変更のリスクに合う検証を選び、失敗の修正後は影響範囲を再確認する。文書だけの変更で全アプリのビルドを必須にしない。完了報告は変更結果・検証結果・残る制約を簡潔に示す。実行していない検証を成功と扱わない。
